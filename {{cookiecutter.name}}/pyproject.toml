[project]
name = "{{ cookiecutter.project_name | replace(' ', '_') }}"
version = "0.1.0"
description = "A project generated from a custom Cookiecutter template"
authors = ["Your Name <your.email@example.com>"]
license = "MIT"
readme = "README.md"

# 의존성을 지정합니다.
dependencies = [
    "pandas"
]

[tool.pdm]
# PDM을 사용하는 경우의 추가 설정 (필요한 경우)
